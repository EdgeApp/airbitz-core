depends="libbitcoin"
inherit lib

source="https://github.com/swansontec/libwallet.git"

build() {
    autoreconf -i
    ./configure --enable-static --disable-shared --host=$cross --prefix=$install_dir
    make
    make install
}

include="include/wallet/*"
lib="lib/libwallet.a"
